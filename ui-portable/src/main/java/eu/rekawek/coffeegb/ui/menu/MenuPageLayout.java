package eu.rekawek.coffeegb.ui.menu;

/** Renderer layout used by a menu page. */
public enum MenuPageLayout {
    /** The standard illustration/details panel beside a vertical option rail. */
    SPLIT,

    /** A single list spanning the page content area. */
    FULL_WIDTH_LIST
}
