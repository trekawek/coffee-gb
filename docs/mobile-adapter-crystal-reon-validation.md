# Japanese Pokémon Crystal and REON validation

This record covers the Phase 3 scenario in issues
[#353](https://github.com/trekawek/coffee-gb/issues/353) and
[#399](https://github.com/trekawek/coffee-gb/issues/399). The Coffee GB comparison uses base
revision [`0d102944a4567e0efba908d078284819b4d34d3e`](https://github.com/trekawek/coffee-gb/tree/0d102944a4567e0efba908d078284819b4d34d3e)
plus this Phase 3 change set. The custom-service observation pins
[`REONTeam/reon@f7bfc0470aed561936b396ed29f2bde50ca601ab`](https://github.com/REONTeam/reon/tree/f7bfc0470aed561936b396ed29f2bde50ca601ab),
licensed MIT.

Commercial-ROM evidence, the reproducible synthetic fixture, and the local REON smoke are kept
separate. No ROM, save, configuration image, account data, credential, checksum, proprietary packet
payload, or server response is included.

## Declared scenario and acceptance boundaries

| Layer | Declared input | Expected evidence | Result boundary |
|---|---|---|---|
| Real game | User-supplied Japanese Pokémon Crystal, synthetic adapter configuration, and no persistent battery write | Wake/poll `4b`; `10` BEGIN; `17` telephone status; valid request ACK, response packet, and response ACK | Confirms that the game recognizes the final link schedule and advances beyond the clean-master wait |
| Deterministic integration | Synthetic configuration, raw-DNS fixture, TCP fixture mapped from guest port 8080 | `10`, `17`, `12`, `21`, `28`, `23`, repeated `15`, `24`, `22`, `13`, `11`; an HTTP reply larger than 253 bytes | Reproducibly covers the complete bounded endpoint/backend sequence, but is not ROM evidence |
| Custom service | Pinned REON checkout isolated on loopback with both runtime gates enabled | Exact-alias DNS, mapped guest TCP/8080, HTTP reply, redacted controller status | A local supporting observation only; it is not reproducible from this repository and is not issue acceptance |

The strict end-to-end criterion is a request generated by the real ROM and observed by REON. The
synthetic fixture and direct service smoke cannot satisfy that criterion by themselves.

## Reproducible committed evidence

The synthetic path is exercised without a ROM or external service:

```text
mvn -pl controller -am -Dtest=MobileAdapterCrystalWireIntegrationTest,MobileAdapterNetworkBackendTest -Dsurefire.failIfNoSpecifiedTests=false test
```

`MobileAdapterCrystalWireIntegrationTest` uses fixture-only names and payloads. It drives the
production endpoint and owner loop through the entire declared command sequence. Its TCP fixture
returns more than one Mobile Adapter packet, proving that a stream reply is retained and drained by
later TRANSFER commands. `MobileAdapterNetworkBackendTest` separately proves that a one-second TCP
read with no data returns a successful empty transfer while preserving the logical connection.

## Real-game result

The clean-master baseline returned idle-high `ff` for every transfer. Crystal sent a valid BEGIN
request, received no acknowledgement or response, and did not issue telephone status.

With this Phase 3 endpoint, Crystal sent its leading `4b` wake/poll and completed these redacted
exchanges using one skipped `d2` turnaround transfer followed by one validated `4b`/`d2` response
gate:

```text
BEGIN             10 -> 90
END               11 -> 91
BEGIN             10 -> 90
TELEPHONE_STATUS  17 -> 97 (three-byte status)
END               11 -> 91
```

The game displayed its Mobile System adapter-check screen. This confirms ROM-level recognition of
the wake byte, request acknowledgements, skipped turnaround byte, validated `4b` response poll,
response stream, and response acknowledgement. It does not confirm later dialing or networking.

## Local REON supporting observation

The REON smoke used Coffee GB's production destination policy and backend against the pinned REON
revision. Exact-alias DNS returned the authorized loopback target, guest TCP port 8080 mapped to the
local HTTP service, and TRANSFER returned the first bounded 253 bytes of a valid HTTP reply. The
redacted status sequence included resolving, connecting, connected, and transferring; the guest
then closed TCP, logged out, hung up, and ended its session.

This smoke is intentionally labeled local and non-reproducible. The pinned REON checkout required a
local compatibility adjustment to the `web/scripts/add_user.php` call arity and a private Compose
override. Neither adjustment, its configuration values, nor generated service data is committed to
Coffee GB. The observation supports the production backend path only.

## Privacy, state, and cleanup boundaries

- All validation networking is explicit, bounded, and restricted to authorized loopback/private
  development mode.
- TCP close, ISP logout, hang-up, end/reset, peripheral replacement, state load, rewind, and
  controller shutdown cancel or release backend ownership as documented in the Mobile Adapter
  contract.
- Save states include the deterministic 256-byte private adapter configuration. A state captured
  during a partial ISP-login packet can also include bounded ID/password parser bytes. Such states
  must remain private or be explicitly redacted.
- Socket, resolver, task, backend generation, request ID, and runtime connection ownership are not
  resurrected from a save state.
- Probe-only REON containers, locally built images, volumes, private overrides, generated
  configuration, and local ROM evidence were removed after validation.

## Remaining verified gaps

The boot-only ROM probe does not dial or enter an online game feature, so it does not issue DNS,
TCP, or HTTP. A gameplay state that can enter a Mobile System feature is still required to meet the
strict ROM-to-REON criterion.

Coffee GB's desktop policy editor preserves its private 256-byte adapter configuration but does not
currently import an owner-provided raw 256-byte image or REON/libmobile 512-byte `MA` + `LM` file.
The synthetic fallback is intentionally not a physical account/configuration image. A focused,
owner-only import flow is therefore still needed before this scenario is usable from the desktop
without an injected test provider.

The current custom-server policy admits one exact guest DNS alias. Any future ROM trace that needs
additional mail, time, or content aliases requires an explicit bounded multi-alias policy rather
than wildcard or ambient DNS access.
