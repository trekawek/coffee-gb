package eu.rekawek.coffeegb.core.debug;

/** Physical graphics hardware and its current cartridge compatibility mode. */
public enum DebugGraphicsHardwareMode {
    DMG,
    CGB_COMPATIBILITY,
    CGB_NATIVE
}
