package eu.rekawek.coffeegb.core.debug;

/** Tri-state feature value used when a safe debug seam cannot inspect mapper internals. */
public enum DebugFeatureState {
    UNKNOWN,
    DISABLED,
    ENABLED
}
