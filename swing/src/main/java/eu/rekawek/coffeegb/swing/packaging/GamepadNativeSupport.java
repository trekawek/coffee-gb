package eu.rekawek.coffeegb.swing.packaging;

/** Whether SDL2 is present in the pinned dependency for a package target. */
public enum GamepadNativeSupport {
    BUNDLED,
    SYSTEM_LIBRARY_REQUIRED
}
